nument = int(input("Enter a number: "))
       
def prime_num():

    prime_list = [a for a in range(1,10) if nument % a == 0]

    if len(prime_list) > 2:

        print("Not a prime.")

    else:

        print("Is a prime")

prime_num()    
